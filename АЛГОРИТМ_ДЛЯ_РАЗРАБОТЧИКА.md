# Алгоритм действий для разработчика

## 🔧 Когда заказчик хочет стать АДМИНОМ

### Получено от заказчика:
```
Мой Telegram ID: 123456789
Прошу добавить меня в админы
```

### Ваши действия:

**Шаг 1: Добавить ID в vars.py**

```bash
# Откройте файл
nano vars.py

# Найдите строку:
admin_ids = [9958633101, 2693757140, 1632759029]

# Добавьте новый ID:
admin_ids = [9958633101, 2693757140, 1632759029, 123456789]

# Сохраните (Ctrl+O, Enter, Ctrl+X)
```

**Шаг 2: Перезапустить бота**

```bash
# На сервере
sudo systemctl restart lgzt_registry-bot

# Проверить что запустился
sudo systemctl status lgzt_registry-bot
```

**Шаг 3: Уведомить заказчика**

```
✅ Готово! Вы добавлены в админы.
Напишите боту /start - увидите меню админа.
```

---

## 👥 Когда заказчик хочет добавить ВОЛОНТЁРА

### Получено от заказчика:
```
Нужно добавить волонтёра:
Telegram ID: 987654321
Имя: Иван Петров
```

### Ваши действия:

**Способ 1: Через скрипт (быстро)**

```bash
python add_volunteer.py 987654321 "Иван Петров"
```

**Способ 2: Через бота**

Если заказчик УЖЕ админ - он может сделать это сам:
1. Бот → /start
2. Кнопка "Добавить волонтёра +"
3. Ввести ID
4. Ввести имя

**Шаг 3: Проверка**

```bash
# Проверить что волонтёр добавлен
sqlite3 app.db "SELECT * FROM user_volunteer WHERE tg_id = 987654321;"
```

**Шаг 4: Уведомить заказчика**

```
✅ Волонтёр добавлен!
Telegram ID: 987654321
Имя: Иван Петров
```

---

## 🗑️ Когда нужно УДАЛИТЬ регистрацию

### Получено от заказчика:
```
Удалите мою регистрацию, ID = 10
```

### Ваши действия:

**Шаг 1: Проверить пользователя**

```bash
python check_user.py 10
```

**Шаг 2: Удалить**

```bash
python delete_user.py 10
```

**Шаг 3: Уведомить заказчика**

```
✅ Регистрация удалена (ID 10)
Можете зарегистрироваться заново через бота
```

---

## 🚀 Деплой изменений на сервер

### После изменения vars.py (добавление админа):

```bash
# 1. Закоммитить изменения
git add vars.py
git commit -m "feat: добавить админа ID 123456789"
git push

# 2. На сервере подтянуть изменения
ssh root@188.225.11.147
cd /var/www/lgzt_registry
git pull

# 3. Перезапустить бота
sudo systemctl restart lgzt_registry-bot

# 4. Проверить логи
sudo journalctl -u lgzt_registry-bot -f
```

### После добавления волонтёра (через скрипт):

Деплой НЕ нужен - волонтёр добавляется в БД напрямую.

---

## 📋 Полезные команды

```bash
# Проверить пользователя
python check_user.py <ID>

# Удалить регистрацию
python delete_user.py <ID>

# Добавить волонтёра
python add_volunteer.py <TELEGRAM_ID> "Имя Фамилия"

# Посмотреть админов
grep "admin_ids" vars.py

# Посмотреть волонтёров в БД
sqlite3 app.db "SELECT * FROM user_volunteer;"

# Перезапустить бота на сервере
sudo systemctl restart lgzt_registry-bot

# Логи бота
sudo journalctl -u lgzt_registry-bot -f

# Статус бота
sudo systemctl status lgzt_registry-bot
```

---

## ⚠️ Важно помнить:

1. **Админы в vars.py** → нужен перезапуск бота после изменений
2. **Волонтёры в БД** → перезапуск НЕ нужен
3. **Всегда делать git push** после изменения vars.py
4. **Telegram ID - это число**, НЕ @username
5. **Проверять логи** после перезапуска: `sudo journalctl -u lgzt_registry-bot -f`

---

## 🆘 Если что-то пошло не так:

**Бот не отвечает после перезапуска:**
```bash
# Проверить статус
sudo systemctl status lgzt_registry-bot

# Посмотреть последние 50 строк логов
sudo journalctl -u lgzt_registry-bot -n 50

# Перезапустить
sudo systemctl restart lgzt_registry-bot
```

**Пользователь не видит админ-меню:**
1. Проверить что ID добавлен в vars.py
2. Проверить что бот перезапущен
3. Попросить написать боту /start заново
4. Проверить логи на ошибки

**Волонтёр не может регистрировать:**
1. Проверить что добавлен в БД: `sqlite3 app.db "SELECT * FROM user_volunteer;"`
2. Попросить волонтёра написать боту /start
3. Проверить логи
