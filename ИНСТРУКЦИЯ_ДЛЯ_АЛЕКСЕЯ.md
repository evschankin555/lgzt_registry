# Инструкция для Алексея Щедрова

## ✅ Выполнено:

1. **Удалена регистрация ID 10** (статус = deleted)
2. Компания очищена
3. Теперь можно зарегистрироваться заново

---

## 📋 Как стать админом:

### Шаг 1: Узнать свой Telegram ID

**Вариант А:** Написать боту @userinfobot - он покажет ваш ID

**Вариант Б:**
1. Написать боту `/start` и зарегистрироваться
2. Попросить разработчика запустить:
   ```bash
   python check_user.py 10
   ```
   (это покажет ваш Telegram ID)

### Шаг 2: Добавить себя в админы

Попросить разработчика/администратора:

1. Открыть файл `vars.py`
2. Найти строку:
   ```python
   admin_ids = [9958633101, 2693757140, 1632759029]
   ```
3. Добавить ваш Telegram ID:
   ```python
   admin_ids = [9958633101, 2693757140, 1632759029, ВАШ_TELEGRAM_ID]
   ```
4. Сохранить файл
5. Перезапустить бота:
   ```bash
   sudo systemctl restart lgzt_registry-bot
   ```

### Шаг 3: Проверка

Напишите боту `/start` - вы увидите меню админа с кнопками статистики и управления.

---

## 👥 Как назначать волонтёров:

### Способ 1: Через бота (рекомендуется)

1. Напишите боту `/start`
2. Выберите "Добавить волонтёра +"
3. Введите Telegram ID человека
4. Введите его имя

### Способ 2: Через скрипт (если знаете Telegram ID)

```bash
python add_volunteer.py TELEGRAM_ID "Имя Фамилия"
```

Пример:
```bash
python add_volunteer.py 123456789 "Иван Иванов"
```

### 📌 Важно:

- **Telegram ID** - это НЕ username (@username)
- **Telegram ID** - это числовой идентификатор (например, 123456789)
- Узнать свой ID можно через бота @userinfobot

---

## 🔍 Полезные команды:

```bash
# Проверить пользователя
python check_user.py 10

# Удалить регистрацию
python delete_user.py 10

# Добавить волонтёра
python add_volunteer.py 123456789 "Имя Фамилия"

# Посмотреть текущих админов
grep "admin_ids" vars.py

# Перезапустить бота на сервере
sudo systemctl restart lgzt_registry-bot
```

---

## 📊 Registry Dashboard

**URL:** https://lgzt.developing-site.ru

**Что можно делать:**
- Просматривать всех пользователей
- Удалять регистрации
- Менять компанию пользователя
- Управлять волонтёрами
- Экспортировать в Excel

---

## 🆘 Если что-то не работает:

1. Проверьте что бот запущен: `sudo systemctl status lgzt_registry-bot`
2. Посмотрите логи: `sudo journalctl -u lgzt_registry-bot -f`
3. Проверьте что Telegram ID правильный (числовой, не username)

---

Подробная документация в файле `ADMIN_GUIDE.md`
